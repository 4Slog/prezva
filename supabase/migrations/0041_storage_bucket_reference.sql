-- B6-1: Storage bucket reference — buckets created via: npm run create-buckets
-- Required buckets:
--   event-photos     (public,  5MB,  images)
--   speaker-handouts (private, 20MB, PDF/PPT)
--   org-assets       (public,  2MB,  images/svg)
--   event-assets     (public,  5MB,  images)
--   user-avatars     (public,  2MB,  images)
--   event-documents  (private, 20MB, PDF/PPT/Word)
SELECT 1;
